[build]
builder = "NIXPACKS"
buildCommand = "go mod download && go build -o server index.go"

[deploy]
startCommand = "./server"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
